{% extends '::base.html.twig' %}
{% block body %}
<h1>Carreras</h1>

<table class="pure-table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>Opciones</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('carrera_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.nombre }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('carrera_show', { 'id': entity.id }) }}">Consultar</a>
                    </li>
                    <li>
                        <a href="{{ path('carrera_edit', { 'id': entity.id }) }}">Editar</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('carrera_new') }}" class="pure-button pure-button-primary">
            Carrera Nueva
        </a>
    </li>
</ul>
{% endblock %}
